steps:
- name: 'gcr.io/cloud-builders/gke-deploy:stable'
  args:
  - run
  - --image=gcr.io/$PROJECT_ID/petclinic:latest
  - --location=us-central1-a
  - --cluster=ct-cluster-haris	
  - --app=petclinic
  - --expose=8080

